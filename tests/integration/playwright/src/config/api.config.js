const DEFAULT_PREFIX_PATH = "/api";
const DEFAULT_PREFIX_URL = "https://node-express-conduit.appspot.com";


export const API_CONFIG = {
    defaultPrefixPath: DEFAULT_PREFIX_PATH,
    services: {
        auth: {
            prefixUrl: DEFAULT_PREFIX_URL,
            prefixPath: DEFAULT_PREFIX_PATH
        },
    }
}; 